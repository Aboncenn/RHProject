{% extends 'base.html.twig' %}

{% block title %}Edition de la campagne{% endblock %}

{% block body %}
    <h1>Editer la campagne</h1>

    {{ include('campagne/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('campagne_index') }}">Retour à la liste</a>

    {{ include('campagne/_delete_form.html.twig') }}
{% endblock %}
