{% extends 'base.html.twig' %}

{% block title %}Editer la campagne{% endblock %}

{% block body %}
    <h1>Edition de la campagne {{ campagne.nom }}</h1>

    {{ include('campagne/_form.html.twig', {'button_label': 'Enregistrer'}) }}

    {{ include('campagne/_delete_form.html.twig') }}

    <a class="btn btn-primary" href="{{ path('campagne_index') }}"><i class="fas fa-arrow-left"></i> Retour à la liste</a>
{% endblock %}
