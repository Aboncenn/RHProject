{% extends 'base.html.twig' %}

{% block body %}
    <section class="home">
        <div class="row justify-content-center">
            <h1>Bienvenue sur RH Project Game {% if user.prenom is defined and user.nom is defined%}{{ user.prenom }} {{ user.nom }}{% endif %}!</h1>

            <img src="{{ asset('images/logo.png') }}" alt="RH Project Game" />
        </div>
    </section>
{% endblock %}
