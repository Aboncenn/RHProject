{% extends 'base.html.twig' %}

{% block body %}
    <section class="container home">
        <div class="row justify-content-center">
            <h1>Bienvenue sur RH Project Game {% if user %}{{ user.username }}{% endif %}!</h1>

            <img src="{{ asset('images/logo.png') }}" alt="RH Project Game" />
        </div>
    </section>
{% endblock %}
